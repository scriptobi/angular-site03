// object class => no constructors needed ?
export class Movie {
  title: string;
  desc: string;
  img: string;

  constructor(title: string, desc: string, img: string) {
    this.title = title;
    this.desc = desc;
    this.img = img;
  }
}
